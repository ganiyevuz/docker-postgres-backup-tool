# Generated
dockerhub.md

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/

docs/superpowers/

# Local-only dev compose (not part of the repo)
docker-compose.local.yml

# Local secrets / environment (never commit real credentials)
.env
.env.*
!.env.example

# Shared default Telegram app build secret — injected at build time only
# (CI writes it from TG_DEFAULT_API_ID / TG_DEFAULT_API_HASH). Never commit.
docker/default-telegram-api
